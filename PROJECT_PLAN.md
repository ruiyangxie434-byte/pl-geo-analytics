# Premier League Insight Agent 开发计划

## 产品目标

第一版完成一条可演示的核心路径：

```text
地图首页 → 球队详情 → 球员榜 → 同位置球员比较 → 示例比赛射门图
```

完成标准以“能运行、能解释数据、能上传 GitHub、能写入简历”为主，不在第一版加入账号、社区、实时比分、预测模型或复杂 3D 地图。

## 阶段 1：项目初始化（已完成）

目标：

- 建立前后端分离目录。
- 初始化 Next.js、TypeScript、Tailwind CSS。
- 初始化 FastAPI、Pydantic。
- 增加 `/api/health`、Swagger、CORS 和统一错误响应。
- 增加环境变量示例、Windows 说明、README 和 Git 规范。

验收：

- [x] 前端可独立启动。
- [x] 后端可独立启动。
- [x] `GET /api/health` 返回 HTTP 200 和统一 JSON。
- [x] 前端可显示后端连接状态。
- [x] `.env` 文件默认被 Git 忽略，`.env.example` 可以提交。

## 阶段 2：测试数据和数据库（已完成）

目标：

- 使用 SQLAlchemy 建立 `clubs`、`players`、`standings`、`player_season_stats`、`matches`、`match_events`。
- 开发环境先使用 SQLite。
- 增加 Alembic 迁移。
- 导入至少 5 支球队及配套球员、积分榜测试数据。
- 实现球队列表、球队详情与积分榜 API。

验收：

- [x] 数据库可通过 `python -m app.database.init_db` 一条命令初始化。
- [x] 外键、坐标约束、唯一约束和必要索引有效。
- [x] API 不从前端硬编码返回业务数据。
- [x] 球员演示数据标记为 `sample`，历史积分榜和球队参考数据分别标记来源类型。
- [x] 后端测试覆盖球队列表、球队详情、积分榜和参数错误。
- [x] 首页可展示加载、成功、失败与重试状态。

## 竞赛冲刺 1：足球分析 Agent MVP（已完成）

- 双球员任务输入与综合、终结、创造、高位逼抢四种分析重点。
- `intent_router`、`player_stats_query`、`per90_calculator`、
  `evidence_ranker` 四步工具链。
- 后端统一计算每90分钟数据和样例百分位。
- 前端展示执行轨迹、动态权重、证据链、置信度与结论边界。
- 支持从中文问题中识别萨卡、帕尔默等样例球员。
- 当前使用可解释规则引擎，运行不依赖任何外部大模型 API。

## 竞赛冲刺 2：千问增强回答层（已完成）

- 根据用户问题自动识别综合、终结、创造或逼抢重点。
- 接入阿里云百炼千问 OpenAI 兼容接口。
- 把本地统计结果、证据和结论边界作为唯一上下文交给千问。
- 使用 JSON 结构化输出，并保持胜者、得分和置信度不可由模型修改。
- 增加 `/api/agent/capabilities`，只暴露配置状态，不暴露密钥。
- 没有密钥、超时或模型响应异常时自动回退本地规则结论。

## 竞赛冲刺 3：真实工具调用、记忆与报告（下一步）

- 让千问通过函数调用自主选择球员查询、指标计算和证据排序工具。
- 保存用户关注球队、位置和战术偏好。
- 支持基于上一次结果继续追问与修正。
- 导出带数据来源和结论边界的球探报告。
- 继续保留本地规则引擎作为无密钥演示和降级路径。

## 阶段 3B：地图首页（已完成）

- [x] React Leaflet + OpenStreetMap。
- [x] 获取后端球队坐标。
- [x] 至少 5 个球场标记、悬停信息和详情跳转。
- [x] 地图标记与球队列表双向联动。
- [x] 桌面和手机基础适配。

## 阶段 4：球队详情和积分榜

- [x] 扩展至 2024-25 完整 20 支球队和球场入口。
- [x] 接入完整 20 队最终积分榜并展示来源与快照日期。
- [x] 支持十列排序、球队联动和冠军/欧冠/降级标记。
- [x] 增加球队搜索、加载、错误和无球员样例空状态。
- [ ] 补充球队赛季汇总、近期赛果与更完整阵容。

## 阶段 5：球员数据分析

- 球员榜、搜索、球队/位置/最低分钟筛选。
- 每 90 分钟数据在后端统一计算并处理除零。
- 球员详情与双人对比基础表。

## 阶段 6：雷达图

- 按位置选择指标。
- 设置最低 450 分钟样本门槛。
- 后端计算同位置球员百分位。
- ECharts 绘制雷达图并说明其相对含义。

## 阶段 7：示例比赛分析

- 引入一场来源明确的公开事件数据。
- 清洗坐标并导入比赛、事件表。
- 展示比赛信息、时间线和射门位置图。
- 明确事件图不等于真实跑动轨迹。

## 阶段 8：项目完善

- 完善响应式布局、骨架屏、错误和空状态。
- 增加测试、项目截图、接口文档、部署说明。
- 检查许可证、数据引用、密钥和大文件。
- 发布 `v1.0.0`。

## 第一版以后再考虑

- xG / xT 模型、球员聚类和相似度推荐。
- 多赛季历史分析。
- 更精细的地形地图。
- 定时更新任务与更完整的数据源。

这些功能不得阻塞第一版。
